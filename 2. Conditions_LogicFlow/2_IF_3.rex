/* If you want to skip the the conditon block execution we can use NOP 
instruction, NOP is null instruction which does nothing NOP( No operation )*/
/* Syntax : 
IF condition THEN
    NOP;
ELSE 
    statement;

*/

IF 1 = 1 THEN
    NOP
ELSE
    say 'nothing will be printed';

exit